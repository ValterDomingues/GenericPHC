# GenericPHC

PHC / SQL Server utilities.

## Open incidences, last 2 business days

`sql/u_reccli_open_last_2_business_days.sql` — `u_reccli.fechada=0` for the
current user, only rows whose `data` falls in the last two weekdays.

See `docs/u_reccli-last-2-business-days.md`.
